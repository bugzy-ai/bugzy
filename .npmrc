# GitHub Packages configuration
# Note: GITHUB_TOKEN must be set as an environment variable
# For publishing, run: pnpm run publish:github
@bugzy-ai:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
